//
//  Arrow.swift
//  
//
//  Created by Vaida on 10/19/22.
//


/// The `to` symbol.
///
/// - Experiment: Maps to `$\to` in LaTeX.
@available(*, unavailable, renamed: "rightArrow")
public let to = LaTeXSymbol("\\to")

/// The `gets` symbol.
///
/// - Experiment: Maps to `$\gets$` in LaTeX.
@available(*, unavailable, renamed: "leftArrow")
public let gets = LaTeXSymbol("\\gets")


/// The `leftarrow` symbol.
///
/// - Experiment: Maps to `$\leftarrow$` in LaTeX.
public let leftArrow = LaTeXSymbol("\\leftarrow")

/// The `rightarrow` symbol.
///
/// - Experiment: Maps to `$\rightarrow$` in LaTeX.
public let rightArrow = LaTeXSymbol("\\rightarrow")

/// The `leftrightarrow` symbol.
///
/// - Experiment: Maps to `$\leftrightarrow$` in LaTeX.
public let leftRightArrow = LaTeXSymbol("\\leftrightarrow")

/// The `leftarrow` symbol.
///
/// - Experiment: Maps to `$\leftarrow$` in LaTeX.
public let LeftArrow = LaTeXSymbol("\\leftarrow")

/// The `rightarrow` symbol.
///
/// - Experiment: Maps to `$\rightarrow$` in LaTeX.
public let RightArrow = LaTeXSymbol("\\rightarrow")

/// The `leftrightarrow` symbol.
///
/// - Experiment: Maps to `$\leftrightarrow$` in LaTeX.
public let LeftRightArrow = LaTeXSymbol("\\leftrightarrow")

/// The `mapsto` symbol.
///
/// - Experiment: Maps to `$\mapsto$` in LaTeX.
public let mapsTo = LaTeXSymbol("\\mapsto")

/// The `hookleftarrow` symbol.
///
/// - Experiment: Maps to `$\hookleftarrow$` in LaTeX.
public let hookLeftArrow = LaTeXSymbol("\\hookleftarrow")

/// The `leftharpoonup` symbol.
///
/// - Experiment: Maps to `$\leftharpoonup$` in LaTeX.
public let leftHarpoonUp = LaTeXSymbol("\\leftharpoonup")

/// The `leftharpoondown` symbol.
///
/// - Experiment: Maps to `$\leftharpoondown$` in LaTeX.
public let leftHarpoonDown = LaTeXSymbol("\\leftharpoondown")

/// The `rightleftharpoons` symbol.
///
/// - Experiment: Maps to `$\rightleftharpoons$` in LaTeX.
public let rightLeftHarpoons = LaTeXSymbol("\\rightleftharpoons")

/// The `longleftarrow` symbol.
///
/// - Experiment: Maps to `$\longleftarrow$` in LaTeX.
public let longLeftArrow = LaTeXSymbol("\\longleftarrow")

/// The `longrightarrow` symbol.
///
/// - Experiment: Maps to `$\longrightarrow$` in LaTeX.
public let longRightArrow = LaTeXSymbol("\\longrightarrow")

/// The `longleftrightarrow` symbol.
///
/// - Experiment: Maps to `$\longleftrightarrow$` in LaTeX.
public let longLeftRightArrow = LaTeXSymbol("\\longleftrightarrow")

/// The `longleftarrow` symbol.
///
/// - Experiment: Maps to `$\longleftarrow$` in LaTeX.
public let LongLeftArrow = LaTeXSymbol("\\longleftarrow")

/// The `longrightarrow` symbol.
///
/// - Experiment: Maps to `$\longrightarrow$` in LaTeX.
public let LongRightArrow = LaTeXSymbol("\\longrightarrow")

/// The `longleftrightarrow` symbol.
///
/// - Experiment: Maps to `$\longleftrightarrow$` in LaTeX.
public let LongLeftRightArrow = LaTeXSymbol("\\longleftrightarrow")

/// The `longmapsto` symbol.
///
/// - Experiment: Maps to `$\longmapsto$` in LaTeX.
public let longMapsTo = LaTeXSymbol("\\longmapsto")

/// The `hookrightarrow` symbol.
///
/// - Experiment: Maps to `$\hookrightarrow$` in LaTeX.
public let hookRightArrow = LaTeXSymbol("\\hookrightarrow")

/// The `rightharpoonup` symbol.
///
/// - Experiment: Maps to `$\rightharpoonup$` in LaTeX.
public let rightHarpoonUp = LaTeXSymbol("\\rightharpoonup")

/// The `rightharpoondown` symbol.
///
/// - Experiment: Maps to `$\rightharpoondown$` in LaTeX.
public let rightHarpoonDown = LaTeXSymbol("\\rightharpoondown")

/// The `iff` symbol.
///
/// - Experiment: Maps to `$\iff$` in LaTeX.
public let iff = LaTeXSymbol("\\iff")

/// The `uparrow` symbol.
///
/// - Experiment: Maps to `$\uparrow$` in LaTeX.
public let upArrow = LaTeXSymbol("\\uparrow")

/// The `downarrow` symbol.
///
/// - Experiment: Maps to `$\downarrow$` in LaTeX.
public let downArrow = LaTeXSymbol("\\downarrow")

/// The `updownarrow` symbol.
///
/// - Experiment: Maps to `$\updownarrow$` in LaTeX.
public let upDownArrow = LaTeXSymbol("\\updownarrow")

/// The `uparrow` symbol.
///
/// - Experiment: Maps to `$\uparrow$` in LaTeX.
public let UpArrow = LaTeXSymbol("\\uparrow")

/// The `downarrow` symbol.
///
/// - Experiment: Maps to `$\downarrow$` in LaTeX.
public let DownArrow = LaTeXSymbol("\\downarrow")

/// The `updownarrow` symbol.
///
/// - Experiment: Maps to `$\updownarrow$` in LaTeX.
public let UpDownArrow = LaTeXSymbol("\\updownarrow")

/// The `nearrow` symbol.
///
/// - Experiment: Maps to `$\nearrow$` in LaTeX.
public let neArrow = LaTeXSymbol("\\nearrow")

/// The `searrow` symbol.
///
/// - Experiment: Maps to `$\searrow$` in LaTeX.
public let seArrow = LaTeXSymbol("\\searrow")

/// The `swarrow` symbol.
///
/// - Experiment: Maps to `$\swarrow$` in LaTeX.
public let swArrow = LaTeXSymbol("\\swarrow")

/// The `nwarrow` symbol.
///
/// - Experiment: Maps to `$\nwarrow$` in LaTeX.
public let nwArrow = LaTeXSymbol("\\nwarrow")

/// The `leadsto` symbol.
///
/// - Experiment: Maps to `$\leadsto$` in LaTeX.
public let leadsTo = LaTeXSymbol("\\leadsto")


//import Foundation
//
//let text = """
///// The `#_#` symbol.
/////
///// - Experiment: Maps to `$\\#_#$` in LaTeX.
//public let ## = LaTeXSymbol("\\\\#_#")\n
//"""
//
//let arrows = ["leftArrow", "rightArrow", "leftRightArrow", "LeftArrow", "RightArrow", "LeftRightArrow", "mapsTo", "hookLeftArrow", "leftHarpoonUp", "leftHarpoonDown", "rightLeftHarpoons", "longLeftArrow", "longRightArrow", "longLeftRightArrow", "LongLeftArrow", "LongRightArrow", "LongLeftRightArrow", "longMapsTo", "hookRightArrow", "rightHarpoonUp", "rightHarpoonDown", "iff", "upArrow", "downArrow", "upDownArrow", "UpArrow", "DownArrow", "UpDownArrow", "neArrow", "seArrow", "swArrow", "nwArrow", "leadsTo"]
//
//for arrow in arrows {
//    print(
//        text
//            .replacingOccurrences(of: "#_#", with: arrow.lowercased())
//            .replacingOccurrences(of: "##", with: arrow)
//    )
//}

