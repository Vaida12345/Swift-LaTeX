//
//  Binary Operator.swift
//  
//
//  Created by Vaida on 10/19/22.
//


/// The `pm` symbol.
///
/// - Experiment: Maps to `$\pm` in LaTeX.
@available(*, unavailable, renamed: "plusMinus")
public let pm = LaTeXSymbol("\\pm")

/// The `mp` symbol.
///
/// - Experiment: Maps to `$\mp` in LaTeX.
@available(*, unavailable, renamed: "minusPlus")
public let mp = LaTeXSymbol("\\mp")

/// The `div` symbol.
///
/// - Experiment: Maps to `$\div` in LaTeX.
@available(*, unavailable, renamed: "divide")
public let div = LaTeXSymbol("\\div")

/// The `plus` symbol.
///
/// - Experiment: Maps to `$\plus$` in LaTeX.
public let plus = LaTeXSymbol("+")

/// The `plusminus` symbol.
///
/// - Experiment: Maps to `$\plusminus$` in LaTeX.
public let plusMinus = LaTeXSymbol("\\pm")

/// The `cdot` symbol.
///
/// - Experiment: Maps to `$\cdot$` in LaTeX.
public let cDot = LaTeXSymbol("\\cdot")

/// The `times` symbol.
///
/// - Experiment: Maps to `$\times$` in LaTeX.
public let times = LaTeXSymbol("\\times")

/// The `cup` symbol.
///
/// - Experiment: Maps to `$\cup$` in LaTeX.
public let cup = LaTeXSymbol("\\cup")

/// The `sqcup` symbol.
///
/// - Experiment: Maps to `$\sqcup$` in LaTeX.
public let squareCup = LaTeXSymbol("\\sqcup")

/// The `vee` symbol.
///
/// - Experiment: Maps to `$\vee$` in LaTeX.
public let vee = LaTeXSymbol("\\vee")

/// The `oplus` symbol.
///
/// - Experiment: Maps to `$\oplus$` in LaTeX.
public let oPlus = LaTeXSymbol("\\oplus")

/// The `odot` symbol.
///
/// - Experiment: Maps to `$\odot$` in LaTeX.
public let oDot = LaTeXSymbol("\\odot")

/// The `otimes` symbol.
///
/// - Experiment: Maps to `$\otimes$` in LaTeX.
public let oTimes = LaTeXSymbol("\\otimes")

/// The `bigtriangleup` symbol.
///
/// - Experiment: Maps to `$\bigtriangleup$` in LaTeX.
public let bigTriangleUp = LaTeXSymbol("\\bigtriangleup")

/// The `lhd` symbol.
///
/// - Experiment: Maps to `$\lhd$` in LaTeX.
public let lhd = LaTeXSymbol("\\lhd")

/// The `unlhd` symbol.
///
/// - Experiment: Maps to `$\unlhd$` in LaTeX.
public let unlhd = LaTeXSymbol("\\unlhd")

/// The `minus` symbol.
///
/// - Experiment: Maps to `$\minus$` in LaTeX.
public let minus = LaTeXSymbol("-")

/// The `minusplus` symbol.
///
/// - Experiment: Maps to `$\minusplus$` in LaTeX.
public let minusPlus = LaTeXSymbol("\\mp")

/// The `divide` symbol.
///
/// - Experiment: Maps to `$\divide$` in LaTeX.
public let divide = LaTeXSymbol("\\div")

/// The `setminus` symbol.
///
/// - Experiment: Maps to `$\setminus$` in LaTeX.
public let setMinus = LaTeXSymbol("\\setminus")

/// The `cap` symbol.
///
/// - Experiment: Maps to `$\cap$` in LaTeX.
public let cap = LaTeXSymbol("\\cap")

/// The `sqcap` symbol.
///
/// - Experiment: Maps to `$\sqcap$` in LaTeX.
public let squareCap = LaTeXSymbol("\\sqcap")

/// The `wedge` symbol.
///
/// - Experiment: Maps to `$\wedge$` in LaTeX.
public let wedge = LaTeXSymbol("\\wedge")

/// The `land` symbol.
///
/// - Experiment: Maps to `$\land$` in LaTeX.
public let land = LaTeXSymbol("\\land")

/// The `ominus` symbol.
///
/// - Experiment: Maps to `$\ominus$` in LaTeX.
public let oMinus = LaTeXSymbol("\\ominus")

/// The `oslash` symbol.
///
/// - Experiment: Maps to `$\oslash$` in LaTeX.
public let oSlash = LaTeXSymbol("\\oslash")

/// The `bigcirc` symbol.
///
/// - Experiment: Maps to `$\bigcirc$` in LaTeX.
public let bigCircle = LaTeXSymbol("\\bigcirc")

/// The `bigtriangledown` symbol.
///
/// - Experiment: Maps to `$\bigtriangledown$` in LaTeX.
public let bigTriangleDown = LaTeXSymbol("\\bigtriangledown")

/// The `rhd` symbol.
///
/// - Experiment: Maps to `$\rhd$` in LaTeX.
public let rhd = LaTeXSymbol("\\rhd")

/// The `unrhd` symbol.
///
/// - Experiment: Maps to `$\unrhd$` in LaTeX.
public let unrhd = LaTeXSymbol("\\unrhd")

/// The `triangleleft` symbol.
///
/// - Experiment: Maps to `$\triangleleft$` in LaTeX.
public let triangleLeft = LaTeXSymbol("\\triangleleft")

/// The `triangleright` symbol.
///
/// - Experiment: Maps to `$\triangleright$` in LaTeX.
public let triangleRight = LaTeXSymbol("\\triangleright")

/// The `star` symbol.
///
/// - Experiment: Maps to `$\star$` in LaTeX.
public let star = LaTeXSymbol("\\star")

/// The `ast` symbol.
///
/// - Experiment: Maps to `$\ast$` in LaTeX.
public let asterisk = LaTeXSymbol("\\ast")

/// The `circle` symbol.
///
/// - Experiment: Maps to `$\circle$` in LaTeX.
public let circle = LaTeXSymbol("\\circle")

/// The `bullet` symbol.
///
/// - Experiment: Maps to `$\bullet$` in LaTeX.
public let bullet = LaTeXSymbol("\\bullet")

/// The `diamond` symbol.
///
/// - Experiment: Maps to `$\diamond$` in LaTeX.
public let diamond = LaTeXSymbol("\\diamond")

/// The `uplus` symbol.
///
/// - Experiment: Maps to `$\uplus$` in LaTeX.
public let uPlus = LaTeXSymbol("\\uplus")

/// The `amalg` symbol.
///
/// - Experiment: Maps to `$\amalg$` in LaTeX.
public let amalg = LaTeXSymbol("\\amalg")

/// The `dagger` symbol.
///
/// - Experiment: Maps to `$\dagger$` in LaTeX.
public let dagger = LaTeXSymbol("\\dagger")

/// The `ddagger` symbol.
///
/// - Experiment: Maps to `$\ddagger$` in LaTeX.
public let ddagger = LaTeXSymbol("\\ddagger")

/// The `wr` symbol.
///
/// - Experiment: Maps to `$\wr$` in LaTeX.
public let wr = LaTeXSymbol("\\wr")


