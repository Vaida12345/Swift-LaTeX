//
//  Binary Relation.swift
//  
//
//  Created by Vaida on 10/19/22.
//


@available(*, unavailable, renamed: "lessLess")
public let ll = LaTeXSymbol("\\ll")

@available(*, unavailable, renamed: "lessEqual")
public let le = LaTeXSymbol("\\le")

@available(*, unavailable, renamed: "greaterEqual")
public let ge = LaTeXSymbol("\\ge")

@available(*, unavailable, renamed: "greaterGreater")
public let gg = LaTeXSymbol("\\gg")

@available(*, unavailable, renamed: "notEqual")
public let ne = LaTeXSymbol("\\ne")



/// The `<` symbol.
///
/// - Experiment: Maps to `$<$` in LaTeX.
public let less = LaTeXSymbol("<")

/// The `le` symbol.
///
/// - Experiment: Maps to `$\le$` in LaTeX.
public let lessEqual = LaTeXSymbol("\\le")

/// The `ll` symbol.
///
/// - Experiment: Maps to `$\ll$` in LaTeX.
public let lessLess = LaTeXSymbol("\\ll")

/// The `prec` symbol.
///
/// - Experiment: Maps to `$\prec$` in LaTeX.
public let prec = LaTeXSymbol("\\prec")

/// The `preceq` symbol.
///
/// - Experiment: Maps to `$\preceq$` in LaTeX.
public let precEqual = LaTeXSymbol("\\preceq")

/// The `subset` symbol.
///
/// - Experiment: Maps to `$\subset$` in LaTeX.
public let subset = LaTeXSymbol("\\subset")

/// The `subseteq` symbol.
///
/// - Experiment: Maps to `$\subseteq$` in LaTeX.
public let subsetEqual = LaTeXSymbol("\\subseteq")

/// The `sqsubset` symbol.
///
/// - Experiment: Maps to `$\sqsubset$` in LaTeX.
public let sqsubset = LaTeXSymbol("\\sqsubset")

/// The `sqsubset` symbol.
///
/// - Experiment: Maps to `$\sqsubset$` in LaTeX.
public let squareSubset = LaTeXSymbol("\\sqsubset")

/// The `sqsubseteq` symbol.
///
/// - Experiment: Maps to `$\sqsubseteq$` in LaTeX.
public let squareSubsetEqual = LaTeXSymbol("\\sqsubseteq")

/// The `in` symbol.
///
/// - Experiment: Maps to `$\in$` in LaTeX.
public let `in` = LaTeXSymbol("\\in")

/// The `vdash` symbol.
///
/// - Experiment: Maps to `$\vdash$` in LaTeX.
public let vDash = LaTeXSymbol("\\vdash")

/// The `mid` symbol.
///
/// - Experiment: Maps to `$\mid$` in LaTeX.
public let mid = LaTeXSymbol("\\mid")

/// The `smile` symbol.
///
/// - Experiment: Maps to `$\smile$` in LaTeX.
public let smile = LaTeXSymbol("\\smile")

/// The `:` symbol.
///
/// - Experiment: Maps to `$:$` in LaTeX.
public let colon = LaTeXSymbol(":")

/// The `>` symbol.
///
/// - Experiment: Maps to `$>$` in LaTeX.
public let greater = LaTeXSymbol(">")

/// The `ge` symbol.
///
/// - Experiment: Maps to `$\ge$` in LaTeX.
public let greaterEqual = LaTeXSymbol("\\ge")

/// The `gg` symbol.
///
/// - Experiment: Maps to `$\gg$` in LaTeX.
public let greaterGreater = LaTeXSymbol("\\gg")

/// The `succ` symbol.
///
/// - Experiment: Maps to `$\succ$` in LaTeX.
public let succ = LaTeXSymbol("\\succ")

/// The `succeq` symbol.
///
/// - Experiment: Maps to `$\succeq$` in LaTeX.
public let succEqual = LaTeXSymbol("\\succeq")

/// The `supset` symbol.
///
/// - Experiment: Maps to `$\supset$` in LaTeX.
public let supSet = LaTeXSymbol("\\supset")

/// The `supseteq` symbol.
///
/// - Experiment: Maps to `$\supseteq$` in LaTeX.
public let supSetEqual = LaTeXSymbol("\\supseteq")

/// The `sqsupset` symbol.
///
/// - Experiment: Maps to `$\sqsupset$` in LaTeX.
public let squareSupSet = LaTeXSymbol("\\sqsupset")

/// The `sqsupseteq` symbol.
///
/// - Experiment: Maps to `$\sqsupseteq$` in LaTeX.
public let squareSupSetEqual = LaTeXSymbol("\\sqsupseteq")

/// The `owns` symbol.
///
/// - Experiment: Maps to `$\owns$` in LaTeX.
public let owns = LaTeXSymbol("\\owns")

/// The `dashv` symbol.
///
/// - Experiment: Maps to `$\dashv$` in LaTeX.
public let dashV = LaTeXSymbol("\\dashv")

/// The `parallel` symbol.
///
/// - Experiment: Maps to `$\parallel$` in LaTeX.
public let parallel = LaTeXSymbol("\\parallel")

/// The `frown` symbol.
///
/// - Experiment: Maps to `$\frown$` in LaTeX.
public let frown = LaTeXSymbol("\\frown")

/// The `notin` symbol.
///
/// - Experiment: Maps to `$\notin$` in LaTeX.
public let notIn = LaTeXSymbol("\\notin")

/// The `=` symbol.
///
/// - Experiment: Maps to `$=$` in LaTeX.
public let equal = LaTeXSymbol("=")

/// The `equiv` symbol.
///
/// - Experiment: Maps to `$\equiv$` in LaTeX.
public let equiv = LaTeXSymbol("\\equiv")

/// The `doteq` symbol.
///
/// - Experiment: Maps to `$\doteq$` in LaTeX.
public let dotEqual = LaTeXSymbol("\\doteq")

/// The `sim` symbol.
///
/// - Experiment: Maps to `$\sim$` in LaTeX.
public let sim = LaTeXSymbol("\\sim")

/// The `simeq` symbol.
///
/// - Experiment: Maps to `$\simeq$` in LaTeX.
public let simEqual = LaTeXSymbol("\\simeq")

/// The `approx` symbol.
///
/// - Experiment: Maps to `$\approx$` in LaTeX.
public let approx = LaTeXSymbol("\\approx")

/// The `cong` symbol.
///
/// - Experiment: Maps to `$\cong$` in LaTeX.
public let cong = LaTeXSymbol("\\cong")

/// The `Join` symbol.
///
/// - Experiment: Maps to `$\Join$` in LaTeX.
public let Join = LaTeXSymbol("\\Join")

/// The `bowtie` symbol.
///
/// - Experiment: Maps to `$\bowtie$` in LaTeX.
public let bowtie = LaTeXSymbol("\\bowtie")

/// The `propto` symbol.
///
/// - Experiment: Maps to `$\propto$` in LaTeX.
public let propTo = LaTeXSymbol("\\propto")

/// The `models` symbol.
///
/// - Experiment: Maps to `$\models$` in LaTeX.
public let models = LaTeXSymbol("\\models")

/// The `perp` symbol.
///
/// - Experiment: Maps to `$\perp$` in LaTeX.
public let perp = LaTeXSymbol("\\perp")

/// The `asymp` symbol.
///
/// - Experiment: Maps to `$\asymp$` in LaTeX.
public let asymp = LaTeXSymbol("\\asymp")

/// The `notequal` symbol.
///
/// - Experiment: Maps to `$\notequal$` in LaTeX.
public let notEqual = LaTeXSymbol("\\notequal")



//import Foundation
//
//let text = """
///// The `#_#` symbol.
/////
///// - Experiment: Maps to `$\\#_#$` in LaTeX.
//public let ## = LaTeXSymbol("\\\\#_#")\n
//"""
//
//let symbols = ["less", "lessEqual", "lessLess", "prec", "precEqual", "subset", "subsetEqual", "sqsubset", "sqSubset", "sqsubsetEqual", "`in`", "vdash", "mid", "smile", "colon", "greater", "greaterEqual", "greaterGreater", "succ", "succEqual", "supset", "supsetEqual", "sqsupset", "sqsupsetEqual", "owns", "dashv", "parallel", "frown", "notIn", "equal", "equiv", "doteq", "sim", "simeq", "approx", "cong", "Join", "bowtie", "propto", "models", "perp", "asymp", "notEqual"]
//
//for symbol in symbols {
//    print(
//        text
//            .replacingOccurrences(of: "#_#", with: symbol.lowercased())
//            .replacingOccurrences(of: "##", with: symbol)
//    )
//}
